/// LiveKit client wrapper — Phase 2.
///
/// Token comes from POST /livekit/token; connect with livekit_client Room.
class LivekitService {
  // Intentionally thin scaffold for Phase 2.
  // Usage sketch:
  //   final res = await api.livekitToken('dm_${a}_$b');
  //   final room = Room();
  //   await room.connect(res['url'], res['token']);
}
